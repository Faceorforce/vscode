#include "node.h"
#include <stdio.h>

//  typedef struct _node
//  {
//      int value;
//      struct _node *next;
//  }node;
 
 int main(int argc, char const *argv[])
 {
     int number;
     do
     {
         scanf("%d",&number);
         if (number != -1)
         {
             /* code */
         }
         
     } while (number != -1);
     
     return 0;
 }